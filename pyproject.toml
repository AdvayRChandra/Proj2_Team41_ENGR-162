[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "proj2_team41_engr_162"
version = "0.1.0"
description = "Particle motion analysis for ENGR-162 Project 2"
dependencies = ["numpy"]
